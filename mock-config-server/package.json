{
  "name": "mock-config-server",
  "version": "1.0.0",
  "private": true,
  "description": "Mock API for per-client build configuration (MonaBit multi-brand pipeline)",
  "type": "module",
  "scripts": {
    "start": "node src/index.js",
    "generate-assets": "node scripts/generate-assets.mjs"
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "devDependencies": {
    "sharp": "^0.33.5"
  }
}
